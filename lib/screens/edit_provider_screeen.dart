import 'package:flutter/material.dart';

class EditProviderScreeen extends StatelessWidget {
  const EditProviderScreeen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('EditProviderScreeen'),
      ),
      body: const Center(
        child: Text('EditProviderScreeen'),
      ),
    );
  }
}